<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <title><%= headTitle %></title>
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
<%- include('partials/navigation', { menuLinks: menuLinks, activeLinkPath: activeLinkPath }) %>
<main>
    <h1 class="heading">Newest Product</h1>
    <% if (Object.keys(newestProduct).length > 0) { %>
        <div>
            Name: <%= newestProduct.name %><br>
            Description: <%= newestProduct.description %>
        </div>
    <% } else { %>
        <div>No data available</div>
    <% } %>
</main>
</body>
</html>